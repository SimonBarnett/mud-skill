# Build/test plan: mud playbook depth (#20)

1. Read issue #20 + current `.grok/skills/**`.
2. Expand playbook markdown; harvest public facts only.
3. Run `tools/Validate-MudSkill.ps1` (BT0).
4. Open PR linking #20.